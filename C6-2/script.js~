const btn = document.getElementById("btnScreenSize")

function getScreenSize() {
  width = window.screen.width;
  height = window.screen.height;
  return `Рамер экрана:\nширина ${width}\nвысота ${height}`;
};

btn.addEventListener("click", () => {
  alert(getScreenSize())
});